sudo: false

language: node_js
node_js:
  - '10'

addons:
  chrome: stable

before_cache:
  - cd ./sd-k9
cache:
  directories:
    - node_modules

install:
  - npm install

script:
  - npm run test -- --watch=false --no-progress 

